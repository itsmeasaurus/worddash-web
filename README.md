# worddash-web

Frontend app repository for WordDash.

## Phase 1 env vars

Copy `.env.example` to `.env.local` for local use.

- `NEXT_PUBLIC_SOCKET_URL`: public URL of realtime Socket.IO service.
- `NEXT_PUBLIC_APP_STATUS`: `enabled` or `maintenance`.
